global:
  resolve_timeout: 1m

route:
  receiver: jenkins-rollback-webhook

receivers:

- name: jenkins-rollback-webhook

  webhook_configs:

  - url: 'http://13.62.95.105:8080/generic-webhook-trigger/invoke?token=ROLLBACK_773134_TOKEN'
    send_resolved: true

    http_config:

      basic_auth:

        username: razi

        password: 116d1818ceef018d36b328b9b628a3bc49
        

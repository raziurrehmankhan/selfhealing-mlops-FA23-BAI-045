apiVersion: v1
kind: Service

metadata:
  name: sentiment-api-service

spec:
  selector:
    app:sentiment-api

  type: NodePort

  ports:
    - nodePort: 32500
      port: 5000
      targetPort: 5000
